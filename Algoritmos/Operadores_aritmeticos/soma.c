#include <stdio.h>

int main()
{
    int n1 = 50;
    int n2 = 45;
    int soma = n1 + n2;
printf("A soma dos numeros eh: %d", soma);

int numeroNovo = 50;
numeroNovo++;
numeroNovo++;

printf("O numero novo vale %i", numeroNovo);

return 0;
}