#include <stdio.h>

int main ()
{
    for (int contador = 0; contador <= 100; contador++){
        if (contador %2 != 0)  {
        
       printf("numero impar: %d  \n", contador);
     }
    }
    return 0;
}